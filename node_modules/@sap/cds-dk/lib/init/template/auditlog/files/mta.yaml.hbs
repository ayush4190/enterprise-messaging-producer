---
modules:
 - name: {{cap.appName}}-srv
   type: {{cap.pLanguage}}
   requires:
    - name: {{cap.appName}}-auditlog

resources:
  - name: {{cap.appName}}-auditlog
    type: org.cloudfoundry.managed-service
    parameters:
      service: auditlog-api
      service-plan: default
