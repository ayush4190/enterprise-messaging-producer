---
modules:
  - name: {{cap.appName}}-srv
    type: {{cap.pLanguage}}
    requires:
      - name: {{cap.appName}}-cf-logging

resources:
  - name: {{cap.appName}}-cf-logging
    type: org.cloudfoundry.managed-service
    parameters:
      service: application-logs
      service-plan: lite