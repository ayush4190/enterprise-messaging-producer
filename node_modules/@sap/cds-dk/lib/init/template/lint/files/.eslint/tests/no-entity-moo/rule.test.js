const { runRuleTester } = require("@sap/eslint-plugin-cds/lib/api");

runRuleTester({
  root: __dirname,
  filename: "schema.cds",
  errors: [
    {
      message: "Entity 'Moo' not allowed!",
      line: 2,
      column: 8,
      endLine: 2,
      endColumn: 11,
    },
  ],
});
