{
  "cds": {
    "requires": {
      "[{{cap.for}}]": {
        "db": {
          "kind": { "#overwrite": "{{#cap.isMultitenant}}hana-mt{{/cap.isMultitenant}}{{^cap.isMultitenant}}hana-cloud{{/cap.isMultitenant}}" }
        }
      },
      "db": {
        "kind": "sql"
      }
    }
  }
}
