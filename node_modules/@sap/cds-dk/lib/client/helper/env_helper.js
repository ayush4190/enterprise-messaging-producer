module.exports = function isBas() {
    const env = Object.entries(process.env);
    return env.some(([key, value]) => key === 'WS_BASE_URL' && value.includes('applicationstudio') || /THEIA_(DEFAULT_)?PLUGINS/.test(key));
}
